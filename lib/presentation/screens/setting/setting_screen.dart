part of '../../presentaion.dart';
class SettingsScreen extends StatelessWidget {
  const SettingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: headingText('Currently, there are no settings available!'),
    );
  }
}